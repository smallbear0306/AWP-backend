<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awp.mapper.CategoryMapper">

    <select id="listByUser" resultType="com.awp.entity.Category">
        SELECT id, user_id, name, type, icon
        FROM category
        WHERE user_id = #{userId}
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY id ASC
    </select>

    <select id="findByIdAndUser" resultType="com.awp.entity.Category">
        SELECT id, user_id, name, type, icon
        FROM category
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.awp.entity.Category"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (user_id, name, type, icon)
        VALUES (#{userId}, #{name}, #{type}, #{icon})
    </insert>

    <update id="update" parameterType="com.awp.entity.Category">
        UPDATE category
        SET name = #{name}, type = #{type}, icon = #{icon}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteByIdAndUser">
        DELETE FROM category WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <select id="countRecords" resultType="int">
        SELECT COUNT(*) FROM record
        WHERE category_id = #{categoryId} AND user_id = #{userId}
    </select>

</mapper>
