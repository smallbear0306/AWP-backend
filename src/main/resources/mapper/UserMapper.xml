<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awp.mapper.UserMapper">

    <select id="findByUsername" resultType="com.awp.entity.User">
        SELECT id, username, password, nickname, create_time
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.awp.entity.User">
        SELECT id, username, password, nickname, create_time
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.awp.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, create_time)
        VALUES (#{username}, #{password}, #{nickname}, NOW())
    </insert>

</mapper>
